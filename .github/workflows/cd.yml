name: CD

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cd-main
  cancel-in-progress: false

jobs:
  deploy:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Fly.io configuration
        id: fly_config
        run: |
          if [[ -z "${{ secrets.FLY_API_TOKEN }}" || -z "${{ vars.FLY_APP_NAME }}" ]]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "Fly.io deploy skipped: configure secret FLY_API_TOKEN and variable FLY_APP_NAME."
          else
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Flyctl
        if: steps.fly_config.outputs.enabled == 'true'
        uses: superfly/flyctl-actions/setup-flyctl@v1

      - name: Deploy to Fly.io
        if: steps.fly_config.outputs.enabled == 'true'
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: flyctl deploy --remote-only --app "${{ vars.FLY_APP_NAME }}"

      - name: Skip deploy (configuration pending)
        if: steps.fly_config.outputs.enabled != 'true'
        run: echo "CD workflow is ready. Add Fly.io settings to enable deployment."
